function lastKNumbersSequance(firstInteger, secondInteger) {
    let resultArray = [];
    let firstElement = 1;

    resultArray.push(firstElement);

    console.log(resultArray);
}

lastKNumbersSequance(6, 3);