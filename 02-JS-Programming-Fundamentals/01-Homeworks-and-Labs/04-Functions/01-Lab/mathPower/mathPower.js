function calculatePowerOfNumber(number, power) {
    let result = number ** power;

    console.log(result);
}

calculatePowerOfNumber(3, 4);


function calculatePowerOfNumber(number, power) {
    let result = Math.pow(number, power);
    console.log(result);
}