function nameGame(input) {
    let playerName = input[0];
    let winnerPoints = 0;

    for (let i = 1; i <= input.length; i++) {
        if(input === "Stop") {
            break;
        }
        for (let j = 1; j <= playerName.length; j++) {
            let pointsCountFirstPlayer = input[j];
        }
    }
}

nameGame(["Ivan",
    "73",
    "20",
    "98",
    "110",
    "Ivo",
    "80",
    "65",
    "87",
    "Stop"])